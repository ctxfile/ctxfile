node_modules/
dist/
packages/core/ui-dist/
*.mcpb
.mcpb-staging/
.env
.env.*
# Templates carry no secrets and document what a deploy needs.
!.env.example
!**/.env.example
*.log
.DS_Store
coverage/
apps/web/.next/
apps/web/out/

# Private, local-only founder docs — never committed, never public.
FOUNDER-ACTIONS.md
TESTING.md
STATUS.md

# Local machine config — per-user, never committed.
.ctxfile.json

# Local wrangler secrets for apps/ping — never committed.
.dev.vars

# Local wrangler dev state (miniflare KV/cache) — never committed.
.wrangler/
